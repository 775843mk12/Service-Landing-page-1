hemeToggle.addEventListene('click',function() {
    body.classList.toggle('dark-mode');
    if
    (body.classList.contains('dark-mode'))
    {
     themeToggle.textContent =   
    }
else {
    themeToggle.texContent =
}

});